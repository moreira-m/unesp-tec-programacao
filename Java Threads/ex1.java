/*
A principal diferença é como as threds são criadas.
No primeiro caso é herdado a classe Thread, diferente do segundo que
implementa a interface Runnable.
As instâncias executadas são diferentes também. 

O primeiro caso é simples porém limita a classe apenas a herdar da classe
Thread.
O segundo caso é um pouco mais flexível em relação a herança e organização
 */